@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
	ViewData["Title"] = @Localizer["ManageUsers"];
}
<h1>@ViewData["Title"]</h1>

<div>
	<table class="table">
		<thead>
			<tr>
				<th>@Localizer["Username"]</th>
				<th>@Localizer["Role"]</th>
				<th>@Localizer["Active"]</th>
				<th>@Localizer["AuthType"]</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			@if (ViewBag.Users != null)
			{
				@foreach (var user in (List<BusinessLayer.DTOs.UserDTO>)ViewBag.Users)
				{
					<tr>
						<td>@user.Username</td>
						<td>@user.Role</td>
						<td>@user.Active</td>
						<td>@user.AuthorizationType</td>
						<td>
							<form method="post">
								@if (user.Role != "Admin")
								{
									<input type="hidden" name="username" value="@user.Username" />
									<button type="submit" formaction="@Url.Action("RemoveUser","User", new {UserId=user.Id})" class="btn btn-danger"> <i class="bi bi-person-dash-fill"></i> @Localizer["DeleteButton"]</button>
									@if (user.Active){
										<button type="submit" formaction="@Url.Action("DeactivateUser","User", new {UserId=user.Id})" class="btn btn-primary"> <i class="bi bi-person-x-fill"></i> @Localizer["DeactivateButton"]</button>
									}
									else{
										<button type="submit" formaction="@Url.Action("ActivateUser","User",  new {UserId=user.Id})" class="btn btn-primary"> <i class="bi bi-person-check-fill"></i> @Localizer["Activate"]</button>
									}
								}
							</form>
						</td>
					</tr>
				}
			}
		</tbody>
	</table>
</div>
